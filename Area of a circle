#include <stdio.h>
int main(){
    float radius, area;
    float pi=3.14159;
    printf("Enter the radius of circle:\n");
    scanf("%f", &radius);

    area = pi*radius*radius;
    printf("Area of a circle: %.2f", area);
    return 0;

}
